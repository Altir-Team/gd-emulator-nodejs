module.exports = {
    sessionGrants: true,
    secret: "Wmfd2893gb7",
    path: "/ffdsdfssdfd" // if your full url (like https://gdjs.altir.xyz/gdEndpoint) length is not matching to 33, then use it / or use nginx/apache/smth else
}